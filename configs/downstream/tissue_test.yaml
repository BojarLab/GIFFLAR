seed: 42
root_dir: /scratch/SCRATCH_SAS/roman/Gothenburg/GIFFLAR/data_new
logs_dir: /scratch/SCRATCH_SAS/roman/Gothenburg/GIFFLAR/logs_new
datasets:
  - name: Tissue
    task: multilabel
pre-transforms:
  ECFPTransform:
  GIFFLARTransform:
  GNNGLYTransform:
  ECFPTransform:
  SweetNetTransform:
  RGCNTransform:
  RandomWalkPE:
    dim: 20
    individual: False
  LaplacianPE:
    dim: 20
    individual: False
model:
  - name: sweetnet
    feat_dim: 128
    hidden_dim: 128
    batch_size: 32
    num_layers: 16
    epochs: 100
    patience: 30
    learning_rate: 0.001
    optimizer: Adam
    suffix:
